"use strict";
// 可索引接口:对数组 对象的约束 (不常用)
let arr = [true, false];
console.log(arr);
let obj11 = {
    name: "小羊"
};
console.log(obj11);
// 接口中的 [index:number]的时候 是对数组的约束
//          [index:string]的时候 是对对象的约束
